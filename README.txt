This lightweight Homey device application adds support through MQTT protocol for Watts Live energy meters. 

It supports using Menno van Grinsven's MQTT Client, or specifying a server directly during pairing, to communicate with an MQTT broker. If using MQTT Client, the client should be already installed and configured.
